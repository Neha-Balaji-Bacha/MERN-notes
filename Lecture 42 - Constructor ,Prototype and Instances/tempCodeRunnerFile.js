const user1 = {
    name: "Vaibhav",
    age: 23,
    city: "Pune",
    printDetails: function () {
        console.log(
            `${this.name} is ${this.age} years old and currently living in ${this.city}`
        );
    }
};

const user2 = {
    name: "Prajwal",
    age: 20,
    city: "Pune",
    printDetails: function () {
        console.log(
            `${this.name} is ${this.age} years old and currently living in ${this.city}`
        );
    }
};

const user3 = {
    name: "Devanshi",
    age: 21,
    city: "Mumbai",
    printDetails: function () {
        console.log(
            `${this.name} is ${this.age} years old and currently living in ${this.city}`
        );
    }
};